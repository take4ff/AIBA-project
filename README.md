# 📊 AIBA — 次世代技術投資分析システム

**Advanced Investment & Behavior Analytics**

世間の熱狂による「割高な高値掴み」を避け、水面下で熱を帯びている次世代技術領域の最適な投資タイミング（押し目・初動）を定量的に検知する。テクニカル指標とセンチメント指標の**乖離**を分析し、データドリブンな投資判断を支援するダッシュボードシステム。

---

## 🚦 現在のステータス

| Phase | 内容 | 状態 |
|---|---|---|
| **Phase 1** | データ基盤・スコア計算・自動化（バックエンド） | ✅ 稼働中 |
| **Phase 2** | データ蓄積とアルゴリズムのキャリブレーション | 🔄 運用中 |
| **Phase 3** | ダッシュボード（フロントエンド） | ✅ 公開済み |

- **本番稼働中**：Supabase にデータ蓄積、GitHub Actions が平日 22:00 UTC に日次実行、Vercel でダッシュボード公開。
- センチメント精度向上のため GitHub API を認証付き（PAT）で利用。

> セットアップ・運用手順の詳細は **[SETUP.md](./SETUP.md)** を参照。

---

## 🎯 監視ターゲットの3層構造（タイムライン別）

時間軸に合わせて領域を3つに分類し、それぞれのフェーズに合った指標で市場を監視する。

| 階層（フェーズ） | 領域の具体例 | 投資の狙い・特徴 |
|---|---|---|
| **第1層：現在のブーム**<br>*(Current Trend)* | 先端半導体（GPU）、生成AI基盤、クラウドインフラ | 既に過熱しているため RSI 等のテクニカル指標を重視し、**「過熱感が収まった押し目」**を的確に狙う。 |
| **第2層：次なる波**<br>*(Next Wave)* | 冷却・電力インフラ、エッジAI・ロボティクス、バイオインフォマティクス | 今後1〜3年で実需が急拡大する領域。テクニカルとセンチメントの**「バランスの取れた成長」**を捉える。 |
| **第3層：未来の開拓地**<br>*(Future Frontier)* | 量子コンピューティング、宇宙インフラ・通信網 | 5〜10年先を見据えた領域。株価が動く前の、GitHub や学術界での**「水面下の研究開発の熱量」**を先行指標として拾う。 |

監視対象の具体的なティッカー・キーワードは [`config/targets.yaml`](./config/targets.yaml) で定義。

---

## 🧮 コア・アルゴリズム（AIBAスコア v1.1）

領域の階層に応じて指標の重み付けを動的に変化させるスコアリングモデル（0〜100）。実装は [`backend/aiba/score.py`](./backend/aiba/score.py)。

**インプットデータ**
- **テクニカル（事実）**：株価・出来高・RSI(14日)・移動平均(25日)乖離率（取得元 `yfinance`）
- **センチメント（先行）**：GitHub リポジトリの増加率・arXiv 論文の出現増加率（取得元 `GitHub API` / `arXiv API`）

**判定ロジック**
- **テクニカル割安感** = RSI割安スコア（低RSIほど高得点）と移動平均乖離スコア（平均より下ほど高得点）の平均。
- **層別重み** = 第1層 (テクニカル0.7 / センチメント0.3)・第2層 (0.5 / 0.5)・第3層 (0.3 / 0.7)。
- **RSIペナルティ** = RSIが50を超えた分を減点し、過熱状態の高値掴みを回避。
- **最終スコア** = 重み付き合算 − RSIペナルティ（0〜100にクランプ）。

---

## 🏗️ システムアーキテクチャ

日々の運用コストをゼロ（無料枠）に抑えつつ、自動化と保守性を高めた構成。

```
yfinance / GitHub API / arXiv API
            │  (収集)
            ▼
   Python バックエンド  ──[GitHub Actions: 日次cron]
   (pandas / requests)
            │  (スコア計算済みの日次サマリーのみInsert)
            ▼
   Supabase (PostgreSQL Free)
            │  (publishableキーで読み取り / RLS保護)
            ▼
   Next.js ダッシュボード (Vercel)
```

- **バックエンド**：Python（`pandas`, `requests`, `yfinance`）— [`backend/`](./backend/)
- **バッチ自動化**：GitHub Actions（cron、米国市場の閉場後に日次実行）— [`.github/workflows/daily.yml`](./.github/workflows/daily.yml)
- **データベース**：Supabase（PostgreSQL Free）。無料枠を活かすため計算済みの日次サマリーのみ保持 — [`db/schema.sql`](./db/schema.sql)
- **フロントエンド**：Next.js（App Router）。階層別ランキング（ヒートマップ）と時系列チャート — [`frontend/`](./frontend/)

### ディレクトリ構成
```
AIBA-project/
├── config/targets.yaml         # 監視対象（3層×ティッカー＋キーワード）
├── db/schema.sql               # Supabase スキーマ（テーブル/ビュー/RLS）
├── backend/                    # Phase 1: 収集・スコア計算・DB投入
│   ├── aiba/{config,technical,sentiment,score,db,pipeline}.py
│   ├── run_daily.py            #   日次バッチのエントリポイント
│   └── backfill.py             #   過去数ヶ月分のバックフィル
├── frontend/                   # Phase 3: Next.js ダッシュボード
└── .github/workflows/daily.yml # Phase 1: 日次自動実行
```

---

## 🗺️ 開発ロードマップ

### ✅ Phase 1: データ基盤の構築と自動化
ティッカー／キーワード確定・Supabase スキーマ・Python パイプライン・GitHub Actions 稼働まで完了。

### 🔄 Phase 2: データ蓄積とキャリブレーション（進行中）
本番データを蓄積しつつ、実際の値動きと突き合わせて `score.py` の重み・閾値を調整する。

### ✅ Phase 3: ダッシュボードの実装
階層切り替え・ランキング・時系列チャートを実装し Vercel で公開済み。

---

## 💡 今後の実装アイデア

### 1. センチメント精度の向上
- [ ] **GitHub指標の高度化**：リポジトリ数だけでなく、スター増加数・コミット頻度・コントリビュータ増減を取り込み「熱量の質」を測る。
- [ ] **データソース追加**：Hacker News（Algolia API・無料）、Google Trends（pytrends）、Reddit などを先行指標に加える。
- [ ] **キーワードの精緻化**：汎用語を除き領域ごとに語数を揃え、単純平均の偏りを解消。
- [ ] **ニュース／決算センチメント**：金融ニュースのポジネガ分析（LLM活用）を組み込む。

### 2. アルゴリズムの高度化（Phase 2の発展）
- [ ] **バックテスト基盤**：過去データでスコアの先行性を検証し、重みを定量的に最適化。
- [ ] **乖離（ダイバージェンス）検知**：テクニカルとセンチメントの乖離が大きい局面を明示的にアラート。
- [ ] **出来高・ボラティリティ指標**の追加（MACD、ボリンジャーバンド等）。
- [ ] **スコアの正規化見直し**：領域横断で比較可能なzスコア化・時系列正規化。

### 3. ダッシュボードの強化
- [ ] **乖離アラート・通知**：閾値超えで Slack / メール / LINE 通知。
- [ ] **個別領域の深掘りビュー**：テクニカルとセンチメントの2軸散布図、寄与度の内訳表示。
- [ ] **期間フィルタ・お気に入り**：表示期間切り替え、ウォッチリスト機能。
- [ ] **モバイル最適化**とダークテーマ以外のテーマ。

### 4. 運用・信頼性
- [ ] **テストの追加**：スコアロジックのユニットテスト、パイプラインの結合テスト（CI化）。
- [ ] **データ品質監視**：取得失敗・欠損のアラート、Actions失敗時の通知。
- [ ] **ログ・メトリクスの可視化**：日次実行の成功率・取得件数の追跡。
- [ ] **対象ユニバースの拡張**：個別銘柄レベルへの分解、新領域（核融合、合成生物学 等）の追加。

### 5. 地域・ユニバースの拡充
- [x] **地域別対応**：同一テーマを Global / 米国 / 日本で並行監視（テクニカルは地域別、センチメントは共通）。ダッシュボードは地域タブで切替。
- [ ] **米国・日本もテーマETFを併用**：現状の代表銘柄（NVDA, 8035.T 等）に加え、地域別テーマETFを対象に追加。
- [ ] **個別株 vs 業界の比較ビュー**：個別銘柄とその業界ETFのAIBAスコアを並べて相対的な割安/割高を見る。

### 6. 表示・予測（メモから）
- [x] **正式名の明示**：ダッシュボードに "Advanced Investment & Behavior Analytics" を表示。
- [ ] **買い場のハイライト**：時系列チャート上で AIBAスコアが買い閾値（例: 60以上）を超えた区間を明示プロット。
- [ ] **1ヶ月先のAIBAスコア予測**：過去系列から先行指標ベースでスコアを予測し、将来の買い場候補を提示。

aibaポートフォリオ, 平均取得評価額
emakis slimeオルカン, 34,535.16 円
emaxis 日経半導体株インデックス, 24,402.15 円
楽天プラスnasdaq100インデックス, 18,938.20 円
NTT, 150.00 円
ローツェ, 4,075.00 円
NF日経エンタメ, 205.00 円
スペースX, 135.0000 USD	
ASTスペースモバイル, 85.0000 USD

地域・業界別のページも作成？個別株もう少し充実させて、対応するETFも表示