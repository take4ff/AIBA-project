-- =============================================================================
-- 未使用ビュー latest_ranking の削除
-- -----------------------------------------------------------------------------
-- Supabase lint「Security Definer View」への対応。
-- このビューは地域対応前の旧仕様で、現在フロントからは使われていない
-- （ランキングは domains＋daily_metrics を直接参照して算出）。削除して解消する。
-- =============================================================================

drop view if exists public.latest_ranking;
